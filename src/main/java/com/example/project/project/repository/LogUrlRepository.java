package com.example.project.project.repository;

import com.example.project.project.model.LogUrl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogUrlRepository extends JpaRepository<LogUrl, Long> {}
